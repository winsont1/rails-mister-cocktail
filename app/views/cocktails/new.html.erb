<div class="container">
  <%= simple_form_for @cocktail do |c| %>
    <%= c.input :name %>
    <%= c.collection_select :ingredients, Ingredient.all, :id, :name %>

    <%= c.submit %>
    <% end %>
</div>
